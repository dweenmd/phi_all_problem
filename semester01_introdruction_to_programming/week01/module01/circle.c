#include<stdio.h>
int main()
{
    // code for calculation the area of circle
    float a,b,area;
    a=3.1416;
    printf("Enter a value of 'r'::");
    scanf("%f",&b);
    area=a*b*b;
    printf("the area of a circle is::%f",area);
    return 0;
}
